# Per-project Ansible config for chapter1
# Video ref: 0:05:31 - Ad-Hoc Commands

[defaults]
# Default inventory so you don't need -i every time
INVENTORY = inventory.ini

# Explicit Python interpreter path on managed hosts (avoids discovery warnings)
interpreter_python = /usr/bin/python3.12
