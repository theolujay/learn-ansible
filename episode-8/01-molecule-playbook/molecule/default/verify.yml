---
- name: Verify
  hosts: instance
  gather_facts: false  # Quicker, if you do not need facts
  tasks:
    - name: Verify that apache is serving web requests
      ansible.builtin.uri:
        url: http://localhost/
        status_code: 200
        # return_content: true
